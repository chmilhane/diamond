public class Diamond.Animations.LerpAngle extends Diamond.AnimationBase
  constructor(index: string|number, goal: number, ...)
    super(...)

    self.animData = {
      index = index,
      goal = goal
    }
  end

  onThink(anim, panel, fraction)
    local { index, goal } = self.animData

    local value = panel[index] ?? Angle()
    panel[index] = LerpAngle(fraction, value, goal)
  end
end